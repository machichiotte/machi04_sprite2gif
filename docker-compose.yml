services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: machi04_sprite2gif:latest
    container_name: machi04_sprite2gif
    ports:
      - "${STREAMLIT_SERVER_PORT:-3004}:3004"
    env_file:
      - .env.production
    restart: always
